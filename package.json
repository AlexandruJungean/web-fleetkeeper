{
  "name": "web-fleetkeeper",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:accessibility": "playwright test --grep @accessibility",
    "test:lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.62.1",
    "serve": "^14.2.6"
  }
}
